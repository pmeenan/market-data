# Copy to .env and fill in. .env is gitignored and loaded automatically by the CLI.

# Tiingo API token (https://www.tiingo.com/account/api/token)
TIINGO_API_TOKEN=

# Where the data warehouse lives (Parquet bars + SQLite metadata).
# Defaults to ./data relative to the current directory if unset.
#MARKET_DATA_DIR=/home/pmeenan/market-data-warehouse
